export interface ICartItem {
    id: number,
    userId: string
    productId: string,
    name: string,
    price: number,
    quantity: number,
    size: string,
    image_url: string,
    sku: string,
}